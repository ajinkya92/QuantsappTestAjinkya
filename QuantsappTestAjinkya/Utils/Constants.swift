import UIKit

struct AppUrls {
    static let BASE_URL = "https://api.androidhive.info/feed/"
    static let GET_FEEDS_URL = BASE_URL + "feed.json"
}

struct AppImageAssets {
    static let NO_PREVIEW_IMAGE = UIImage(named: "no image available")
}
